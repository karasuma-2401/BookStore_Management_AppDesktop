<UserControl x:Class="BookStore_Management_AppDesktop.Views.UserControls.BookCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="360" d:DesignWidth="195">

    <UserControl.Resources>
        <Style x:Key="ElevatedCardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="#E2E8F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="Width" Value="185" />
            <Setter Property="Cursor" Value="Hand" />

            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#0F172A" Opacity="0.05" BlurRadius="10" ShadowDepth="3" Direction="270"/>
                </Setter.Value>
            </Setter>

            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform X="0" Y="0"/>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#94A3B8" />

                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#0F172A" Opacity="0.14" BlurRadius="20" ShadowDepth="8" Direction="270"/>
                        </Setter.Value>
                    </Setter>

                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform X="0" Y="-5"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource ElevatedCardBorderStyle}">

        <Border.InputBindings>
            <MouseBinding Gesture="LeftClick" 
                          Command="{Binding DataContext.ViewBookDetailCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                          CommandParameter="{Binding}" />
        </Border.InputBindings>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="8" ClipToBounds="True" Margin="0,0,0,8" Background="{DynamicResource ContentBackgroundBrush}">
                <Image Height="185" Stretch="UniformToFill"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       Source="{Binding ImagePath, IsAsync=True, TargetNullValue='https://via.placeholder.com/185x220?text=No+Image', FallbackValue='https://via.placeholder.com/185x220?text=No+Image'}">
                </Image>
            </Border>

            <TextBlock Grid.Row="1" Text="{Binding Title}" 
                       Style="{StaticResource BookTitleTextStyle}"
                       Height="38" FontSize="14"
                       TextTrimming="CharacterEllipsis" 
                       TextWrapping="Wrap"
                       Margin="0,0,0,4"/>

            <TextBlock Grid.Row="2" Text="{Binding DisplayAuthorNames, TargetNullValue='Unknown Author'}" 
                       FontSize="11" 
                       FontWeight="Medium"
                       Foreground="{DynamicResource TextSecondBrush}"
                       TextTrimming="CharacterEllipsis"
                       Margin="0,0,0,2"/>

            <TextBlock Grid.Row="3" Text="{Binding DisplayCategoryNames, TargetNullValue='Uncategorized'}" 
                       FontSize="10" 
                       FontStyle="Italic"
                       Foreground="{DynamicResource TextSecondBrush}"
                       TextTrimming="CharacterEllipsis"
                       Margin="0,0,0,8"/>

            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom">
                <TextBlock Text="{Binding Price, StringFormat='{}{0:#,0}', TargetNullValue='0'}" 
                           Style="{StaticResource PriceTextStyle}"
                           FontSize="15"
                           FontWeight="Bold"/>
                <TextBlock Text=" đ" 
                           FontSize="11" 
                           FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextSecondBrush}"
                           VerticalAlignment="Bottom"
                           Margin="1,0,0,2"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
<Window x:Class="BookStore_Management_AppDesktop.Views.Windows.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Confirmation" 
        MinHeight="170" Width="450" 
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True" 
        WindowStyle="None" 
        Background="Transparent">

    <Border Background="{DynamicResource TextPrimaryBrush}" 
            CornerRadius="12" 
            BorderBrush="{DynamicResource TextPrimaryBrush}"
            BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.15" ShadowDepth="2"/>
        </Border.Effect>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0">
                <Grid Margin="0,0,16,0">
                    <ui:SymbolIcon x:Name="DangerIcon" Symbol="Delete24" Filled="True" FontSize="28" Foreground="{DynamicResource ErrorPrimaryBrush}" Visibility="Collapsed"/>
                    <ui:SymbolIcon x:Name="InfoIcon" Symbol="QuestionCircle24" Filled="True" FontSize="28" Foreground="{DynamicResource NavyAccentBrush}" Visibility="Visible"/>
                </Grid>

                <TextBlock x:Name="txtMessage" Text="Are you sure?"
                           FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextOnLightBackgroudBrush}" TextWrapping="Wrap" MaxWidth="320"/>
            </StackPanel>

            <UniformGrid Grid.Row="1" Columns="2" Margin="0,24,0,0" VerticalAlignment="Bottom">
                <Button Content="Cancel" Height="42" Margin="0,0,8,0" Cursor="Hand" IsCancel="True" Click="Cancel_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{DynamicResource TextMutedBrush}" BorderThickness="1" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#10000000"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="btnConfirm" Content="Confirm" Height="42" Margin="8,0,0,0" Cursor="Hand" IsDefault="True" Click="Confirm_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{DynamicResource NavyAccentBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Opacity" Value="0.8"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </UniformGrid>
        </Grid>
    </Border>
</Window>
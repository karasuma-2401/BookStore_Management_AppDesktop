<Window x:Class="BookStore_Management_AppDesktop.Views.Windows.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Notification" SizeToContent="WidthAndHeight" 
        Width="440" MinHeight="170" MaxWidth="520"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <!-- Style for Primary Actions (OK, Yes) -->
        <Style x:Key="DialogPrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.12" Color="#000000"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for Secondary Actions (Cancel, No) -->
        <Style x:Key="DialogSecondaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#F8FAFC"/>
                                <Setter TargetName="BtnBorder" Property="BorderBrush" Value="#94A3B8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <!-- Glow drop shadow border -->
        <Border CornerRadius="16" Background="White" x:Name="CardBorder" BorderThickness="1.5" BorderBrush="#E2E8F0">
            <Border.Effect>
                <DropShadowEffect x:Name="CardShadow" BlurRadius="25" ShadowDepth="4" Opacity="0.12" Color="#0F172A"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header Bar -->
                <Grid Grid.Row="0" Margin="20,16,16,8">
                    <TextBlock x:Name="txtTitle" Text="Notification" FontWeight="Bold" FontSize="15" Foreground="#1E293B" VerticalAlignment="Center"/>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Width="28" Height="28" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnClose_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="CloseBorder" Background="Transparent" CornerRadius="14">
                                                <ui:SymbolIcon Symbol="Dismiss24" FontSize="13" Foreground="#94A3B8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="CloseBorder" Property="Background" Value="#F1F5F9"/>
                                                    <Setter Property="Foreground" Value="#475569"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <!-- Body Section -->
                <Grid Grid.Row="1" Margin="20,8,20,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Circular Icon Badge -->
                    <Border x:Name="BadgeBorder" Grid.Column="0" Width="48" Height="48" CornerRadius="24" BorderThickness="1.5" VerticalAlignment="Top" Margin="0,0,16,0" Background="#EFF6FF" BorderBrush="#BFDBFE">
                        <ui:SymbolIcon x:Name="BadgeIcon" Symbol="Info24" Filled="True" FontSize="20" Foreground="#2563EB" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <!-- Message Text -->
                    <TextBlock x:Name="txtMessage" Grid.Column="1" Text="Content message goes here" FontSize="14.5" Foreground="#334155" LineHeight="20" TextWrapping="Wrap" VerticalAlignment="Center"/>
                </Grid>

                <!-- Footer Section (Buttons Panel) -->
                <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="0,0,16,16" Padding="20,14" BorderBrush="#F1F5F9" BorderThickness="0,1,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" x:Name="ButtonPanel">
                        <Button x:Name="BtnYes" Content="Yes" Style="{StaticResource DialogPrimaryButtonStyle}" Margin="0,0,10,0" Click="BtnYes_Click" Visibility="Collapsed"/>
                        <Button x:Name="BtnNo" Content="No" Style="{StaticResource DialogSecondaryButtonStyle}" Margin="0,0,10,0" Click="BtnNo_Click" Visibility="Collapsed"/>
                        <Button x:Name="BtnOk" Content="OK" Style="{StaticResource DialogPrimaryButtonStyle}" Margin="0,0,10,0" Click="BtnOk_Click" Visibility="Visible"/>
                        <Button x:Name="BtnCancel" Content="Cancel" Style="{StaticResource DialogSecondaryButtonStyle}" Click="BtnCancel_Click" Visibility="Collapsed"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
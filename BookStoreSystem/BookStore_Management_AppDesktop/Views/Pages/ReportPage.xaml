<Page
    x:Class="BookStore_Management_AppDesktop.Views.Pages.ReportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:BookStore_Management_AppDesktop.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:BookStore_Management_AppDesktop.ViewModels"
    Title="Report Dashboard"
    d:DataContext="{d:DesignInstance Type=viewmodels:ReportViewModel}"
    d:DesignHeight="768"
    d:DesignWidth="1100"
    Background="{DynamicResource ContentBackgroundBrush}"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Padding="25,15,25,15"
            Background="{DynamicResource SidebarBackgroundBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock
                        FontSize="26"
                        FontWeight="Black"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Text="Financial Report" />
                    <TextBlock
                        Margin="0,5,0,0"
                        FontSize="14"
                        Foreground="{DynamicResource TextSecondaryBrush}"
                        Text="Overview of revenue, costs and top selling books" />
                </StackPanel>

                <StackPanel
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <TextBlock
                        Margin="0,0,10,0"
                        VerticalAlignment="Center"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Text="Month:" />
                    <ComboBox
                        Width="80"
                        Margin="0,0,20,0"
                        Background="{DynamicResource ContentBackgroundBrush}"
                        ItemsSource="{Binding Months}"
                        SelectedItem="{Binding SelectedMonth}" />

                    <TextBlock
                        Margin="0,0,10,0"
                        VerticalAlignment="Center"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Text="Year:" />
                    <ComboBox
                        Width="90"
                        Margin="0,0,20,0"
                        Background="{DynamicResource ContentBackgroundBrush}"
                        ItemsSource="{Binding Years}"
                        SelectedItem="{Binding SelectedYear}" />

                    <ui:Button
                        Padding="20,8"
                        Margin="0,0,10,0"
                        Appearance="Primary"
                        Background="{DynamicResource NavyAccentBrush}"
                        Command="{Binding GenerateReportCommand}"
                        Content="Generate Report" />

                    <ui:Button
                        Padding="20,8"
                        Appearance="Secondary"
                        Background="{DynamicResource SidebarAccentBrush}"
                        Command="{Binding ExportToExcelCommand}"
                        Content="Export to Excel" />
                </StackPanel>
            </Grid>
        </Border>

        <UniformGrid
            Grid.Row="1"
            Margin="20,20,20,10"
            Columns="4"
            Rows="1">
            <Border
                Margin="5"
                Padding="20"
                Background="{DynamicResource SidebarBackgroundBrush}"
                CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.1"
                        ShadowDepth="2"
                        Color="Black" />
                </Border.Effect>
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="12"
                        FontWeight="Bold"
                        Foreground="{DynamicResource TextSecondaryBrush}"
                        Text="TOTAL REVENUE" />
                    <TextBlock
                        FontSize="28"
                        FontWeight="Black"
                        Foreground="{DynamicResource SuccessPrimaryBrush}"
                        Text="{Binding TotalRevenue, StringFormat={}{0:N0} ₫}" />
                </StackPanel>
            </Border>

            <Border
                Margin="5"
                Padding="20"
                Background="{DynamicResource SidebarBackgroundBrush}"
                CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.1"
                        ShadowDepth="2"
                        Color="Black" />
                </Border.Effect>
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="12"
                        FontWeight="Bold"
                        Foreground="{DynamicResource TextSecondaryBrush}"
                        Text="TOTAL IMPORT COST" />
                    <TextBlock
                        FontSize="28"
                        FontWeight="Black"
                        Foreground="{DynamicResource ErrorPrimaryBrush}"
                        Text="{Binding TotalImportCost, StringFormat={}{0:N0} ₫}" />
                </StackPanel>
            </Border>

            <Border
                Margin="5"
                Padding="20"
                Background="{DynamicResource SidebarBackgroundBrush}"
                CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.1"
                        ShadowDepth="2"
                        Color="Black" />
                </Border.Effect>
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="12"
                        FontWeight="Bold"
                        Foreground="{DynamicResource TextSecondaryBrush}"
                        Text="NET PROFIT" />
                    <TextBlock
                        FontSize="28"
                        FontWeight="Black"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Text="{Binding Profit, StringFormat={}{0:N0} ₫}" />
                </StackPanel>
            </Border>

            <Border
                Margin="5"
                Padding="20"
                Background="{DynamicResource SidebarBackgroundBrush}"
                CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.1"
                        ShadowDepth="2"
                        Color="Black" />
                </Border.Effect>
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="12"
                        FontWeight="Bold"
                        Foreground="{DynamicResource TextSecondaryBrush}"
                        Text="BOOKS SOLD" />
                    <TextBlock
                        FontSize="28"
                        FontWeight="Black"
                        Foreground="{DynamicResource SidebarAccentBrush}"
                        Text="{Binding TotalBooksSold}" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2" Margin="20,10,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Column="0"
                Margin="5,0,10,0"
                Padding="20"
                Background="{DynamicResource SidebarBackgroundBrush}"
                CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.1"
                        ShadowDepth="2"
                        Color="Black" />
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Margin="0,0,0,15"
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Text="Daily Revenue Trend" />

                    <lvc:CartesianChart
                        Grid.Row="1"
                        Series="{Binding RevenueSeries}"
                        TooltipPosition="Hidden"
                        XAxes="{Binding XAxes}" />

                    <ui:ProgressRing
                        Grid.Row="1"
                        Width="60"
                        Height="60"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource NavyAccentBrush}"
                        IsIndeterminate="True"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />
                </Grid>
            </Border>

            <Border
                Grid.Column="1"
                Margin="10,0,5,0"
                Padding="20"
                Background="{DynamicResource SidebarBackgroundBrush}"
                CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.1"
                        ShadowDepth="2"
                        Color="Black" />
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock
                        Grid.Row="0"
                        Margin="0,0,0,15"
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Text="Top 5 Selling Books" />

                    <DataGrid
                        Grid.Row="1"
                        AutoGenerateColumns="False"
                        Background="Transparent"
                        BorderThickness="0"
                        GridLinesVisibility="Horizontal"
                        HeadersVisibility="Column"
                        HorizontalGridLinesBrush="{DynamicResource DividerBrush}"
                        IsReadOnly="True"
                        ItemsSource="{Binding TopBooks}"
                        RowBackground="Transparent">
                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Title}"
                                Foreground="{DynamicResource TextPrimaryBrush}"
                                Header="Book Title">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn
                                Width="60"
                                Binding="{Binding TotalSold}"
                                Foreground="{DynamicResource SidebarAccentBrush}"
                                Header="Sold">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                        <Setter Property="FontWeight" Value="Bold" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn
                                Width="110"
                                Binding="{Binding RevenueGenerated, StringFormat={}{0:N0} ₫}"
                                Foreground="{DynamicResource SuccessPrimaryBrush}"
                                Header="Revenue">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                        <Setter Property="FontWeight" Value="Bold" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
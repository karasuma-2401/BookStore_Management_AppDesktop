<Page x:Class="BookStore_Management_AppDesktop.Views.Pages.InvoiceViews.InvoicePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Background="{DynamicResource ContentBackgroundBrush}"
      d:DesignHeight="700"
      d:DesignWidth="1000"
      Title="InvoicePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource SidebarBackgroundBrush}"
                Padding="25,15">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Invoice Management"
                           FontSize="26"
                           FontWeight="Black"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">

                    <ui:TextBox 
                                PlaceholderText="Search invoices..."
                                Width="250"
                                Margin="0,0,15,0"
                                VerticalAlignment="Center"
                                Padding="12,8"
                                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                    <ui:Button x:Name="FilterButton"
                               Icon="{ui:SymbolIcon Filter24}" 
                               Appearance="Secondary" 
                               Margin="0,0,10,0" 
                               Click="FilterButton_Click">
                        <ui:Button.ContextMenu>
                            <ContextMenu x:Name="FilterMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Total Amount"
                                          IsChecked="{Binding IsTotalAmountSelected, Mode=OneWay}"
                                          Command="{Binding ChangeFilterCommand}"
                                          CommandParameter="Total Amount"/>
                                <MenuItem Header="Invoice Date"
                                          IsChecked="{Binding IsInvoiceDateSelected, Mode=OneWay}"
                                          Command="{Binding ChangeFilterCommand}"
                                          CommandParameter="Invoice Date"/>
                            </ContextMenu>
                        </ui:Button.ContextMenu>
                    </ui:Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="{DynamicResource CardBackgroundBrush}"
                Margin="20"
                CornerRadius="8"
                BorderBrush="#E2E8F0"
                BorderThickness="1">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="InvoiceDataGrid"
                          ItemsSource="{Binding Invoices}"
                          AutoGenerateColumns="False"
                          Grid.Row="0"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          RowHeight="70"
                          HeadersVisibility="Column"
                          Foreground="#1E293B"
                          Background="Transparent"
                          CanUserResizeColumns="False"
                          RowBackground="{DynamicResource CardBackgroundBrush}"
                          AlternatingRowBackground="{DynamicResource ContentBackgroundBrush}"
                          HorizontalGridLinesBrush="#E2E8F0"
                          VerticalGridLinesBrush="Transparent"
                          FontSize="15"
                          Cursor="Hand"
                          EnableRowVirtualization="True"
                          PreviewMouseLeftButtonDown="InvoiceDataGrid_PreviewMouseLeftButtonDown">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="12,0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource ErrorPrimaryBrush}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource ContentBackgroundBrush}"/>
                            <Setter Property="Height" Value="50"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ContentBackgroundBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Invoice ID" Binding="{Binding InvoiceId}" Width="100"/>
                        <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="180"/>
                        <DataGridTextColumn Header="Staff" Binding="{Binding StaffName}" Width="180"/>
                        <DataGridTextColumn Header="Invoice Date" Binding="{Binding InvoiceDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="180"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat='{}{0:N0} VNĐ'}" Width="140"/>
                        <DataGridTextColumn Header="Items" Binding="{Binding TotalItems}" Width="100"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140"/>

                        <DataGridTemplateColumn Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Click="MoreButton_Click"
                                            Background="Transparent" 
                                            Padding="0" 
                                            BorderThickness="0"
                                            Cursor="Hand">
                                        <TextBlock Text="⋮⋮⋮" 
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource TextOnLightBackgroudBrush}"
                                                   VerticalAlignment="Center"/>
                                        <Button.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="View Detail" 
                                                          Command="{Binding DataContext.ViewInvoiceDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}"
                                                          Foreground="{DynamicResource SuccessPrimaryBrush}"/>
                                                <Separator/>
                                                <MenuItem Header="Cancel" 
                                                          Command="{Binding DataContext.CancelInvoiceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}"
                                                          Foreground="{DynamicResource ErrorPrimaryBrush}"/>
                                            </ContextMenu>
                                        </Button.ContextMenu>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Row="1" Margin="0 30 20 20">

                    <TextBlock Text="Lines per page" 
                               VerticalAlignment="Center" 
                               Foreground="{DynamicResource TextSecondaryBrush}" 
                               FontSize="13" 
                               Margin="0,0,15,0"/>

                    <Grid VerticalAlignment="Center" Margin="0,0,40,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding PageSize}" 
                                       VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextOnLightBackgroudBrush}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Margin="0,0,5,0"/>
                            <ui:SymbolIcon Symbol="ChevronDown24" 
                                           Filled="False" 
                                           Foreground="{DynamicResource TextSecondaryBrush}" 
                                           FontSize="10" 
                                           VerticalAlignment="Center"/>
                        </StackPanel>

                        <ComboBox ItemsSource="{Binding PageSizeOptions}" 
                                  SelectedItem="{Binding PageSize}" 
                                  Width="50" 
                                  Height="25" 
                                  Background="Transparent" 
                                  BorderThickness="0" 
                                  Opacity="0.01" 
                                  Cursor="Hand"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,30,0">
                        <TextBlock Text="{Binding CurrentPageStart}" Foreground="{DynamicResource TextOnLightBackgroudBrush}" FontSize="13"/>
                        <TextBlock Text="-" Foreground="{DynamicResource TextOnLightBackgroudBrush}" Margin="2,0"/>
                        <TextBlock Text="{Binding CurrentPageEnd}" Foreground="{DynamicResource TextOnLightBackgroudBrush}" FontSize="13"/>
                        <TextBlock Text="of" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13" Margin="8,0"/>
                        <TextBlock Text="{Binding TotalInvoices}" Foreground="{DynamicResource TextOnLightBackgroudBrush}" FontSize="13"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <ui:Button Icon="{ui:SymbolIcon Symbol=ChevronLeft24}" 
                                   Appearance="Transparent" 
                                   Foreground="{DynamicResource TextSecondaryBrush}" 
                                   Padding="5" 
                                   Command="{Binding PreviousPageCommand}"/>

                        <ui:Button Icon="{ui:SymbolIcon Symbol=ChevronRight24}" 
                                   Appearance="Transparent" 
                                   Foreground="{DynamicResource TextSecondaryBrush}" 
                                   Padding="5" 
                                   Command="{Binding NextPageCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
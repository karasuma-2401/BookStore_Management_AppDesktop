<Page x:Class="BookStore_Management_AppDesktop.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="SettingsPage"
      Background="{DynamicResource ContentBackgroundBrush}">

    <Page.Resources>
        <Style TargetType="ui:TextBox" BasedOn="{StaticResource {x:Type ui:TextBox}}">
            <Style.Resources>
                <SolidColorBrush x:Key="TextControlForeground" Color="#1E293B"/>
                <SolidColorBrush x:Key="TextControlForegroundPointerOver" Color="#1E293B"/>
                <SolidColorBrush x:Key="TextControlForegroundFocused" Color="#1E293B"/>
                <SolidColorBrush x:Key="TextControlPlaceholderForeground" Color="#94A3B8"/>
                <SolidColorBrush x:Key="TextControlPlaceholderForegroundFocused" Color="#94A3B8"/>
                <SolidColorBrush x:Key="TextControlBackground" Color="Transparent"/>
                <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="Transparent"/>
                <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="Transparent"/>
            </Style.Resources>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource SidebarBackgroundBrush}" Padding="25,15">
            <TextBlock Text="VOUCHER MANAGEMENT" Style="{StaticResource HeaderTextStyle}" Foreground="{DynamicResource TextPrimaryBrush}"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">

                <TextBlock Text="Voucher Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource TextOnCardBrush}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="380"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ui:Card Grid.Column="0" Margin="0,0,20,0" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Create New Voucher" FontSize="14" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1E293B"/>

                            <TextBlock Text="Code" FontSize="12" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                            <Border Height="40" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Margin="0,0,0,15">
                                <ui:TextBox Text="{Binding NewVoucherCode}" PlaceholderText="e.g. SUMMER2026" BorderThickness="0" Background="Transparent" Foreground="#1E293B" CaretBrush="#1E293B"/>
                            </Border>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="Discount (%)" FontSize="12" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                                    <Border Height="40" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6">
                                        <ui:TextBox Text="{Binding NewVoucherDiscountPercent}" PlaceholderText="0" BorderThickness="0" Background="Transparent" Foreground="#1E293B" CaretBrush="#1E293B"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="Discount (đ)" FontSize="12" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                                    <Border Height="40" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6">
                                        <ui:TextBox Text="{Binding NewVoucherDiscountAmount}" PlaceholderText="0" BorderThickness="0" Background="Transparent" Foreground="#1E293B" CaretBrush="#1E293B"/>
                                    </Border>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Expiry Date" FontSize="12" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                            <Border Height="40" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Margin="0,0,0,20">
                                <ui:TextBox Text="{Binding NewVoucherExpiryDate, StringFormat='yyyy-MM-dd'}" BorderThickness="0" Background="Transparent" Foreground="#1E293B" CaretBrush="#1E293B"/>
                            </Border>

                            <TextBlock Text="Usage Limit" FontSize="12" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                            <Border Height="40" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Margin="0,0,0,20">
                                <ui:TextBox Text="{Binding NewVoucherUsageLimit}" PlaceholderText="e.g. 100" BorderThickness="0" Background="Transparent" Foreground="#1E293B" CaretBrush="#1E293B"/>
                            </Border>

                            <ui:Button Content="CREATE VOUCHER" Command="{Binding CreateVoucherCommand}" Appearance="Primary" HorizontalAlignment="Stretch" Height="40"/>
                        </StackPanel>
                    </ui:Card>

                    <ui:Card Grid.Column="1" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Active Vouchers" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource TextOnCardBrush}"/>
                            <ItemsControl ItemsSource="{Binding Vouchers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,8" Padding="12" CornerRadius="8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Code}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextOnCardBrush}" Margin="0,0,0,4"/>
                                                    <TextBlock Text="{Binding ExpiryDate, StringFormat='Expires: {0:yyyy-MM-dd}'}" FontSize="11" Foreground="{DynamicResource TextBodyBrush}"/>
                                                </StackPanel>
                                                <ui:Button Grid.Column="1" 
                                                       Icon="{ui:SymbolIcon Delete24}" 
                                                       Command="{Binding DataContext.DeleteVoucherCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                                       CommandParameter="{Binding}" 
                                                       Appearance="Transparent" 
                                                       Foreground="{DynamicResource TextSecondBrush}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ui:Card>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
<Page x:Class="BookStore_Management_AppDesktop.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="SettingsPage"
      Background="{DynamicResource ContentBackgroundBrush}">

    <Page.Resources>
        <Style TargetType="ui:TextBox" BasedOn="{StaticResource {x:Type ui:TextBox}}">
            <Style.Resources>
                <SolidColorBrush x:Key="TextControlForeground" Color="{DynamicResource TextOnCardColor}"/>
                <SolidColorBrush x:Key="TextControlForegroundPointerOver" Color="{DynamicResource TextOnCardColor}"/>
                <SolidColorBrush x:Key="TextControlForegroundFocused" Color="{DynamicResource TextOnCardColor}"/>
                <SolidColorBrush x:Key="TextControlPlaceholderForeground" Color="{DynamicResource SlateBorderColor}"/>
                <SolidColorBrush x:Key="TextControlPlaceholderForegroundFocused" Color="{DynamicResource SlateBorderColor}"/>
                <SolidColorBrush x:Key="TextControlBackground" Color="Transparent"/>
                <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="Transparent"/>
                <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="Transparent"/>
            </Style.Resources>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource SidebarBackgroundBrush}" Padding="25,15">
            <TextBlock Text="VOUCHER MANAGEMENT" Style="{StaticResource HeaderTextStyle}" Foreground="{DynamicResource TextPrimaryBrush}"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">

                <TextBlock Text="Voucher Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource TextOnCardBrush}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="380"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ui:Card Grid.Column="0" Margin="0,0,20,0" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Create New Voucher" FontSize="14" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource TextOnCardBrush}"/>

                            <TextBlock Text="Code" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SlateLightBrush}" Margin="0,0,0,5"/>
                            <Border Height="40" Background="White" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="6" Margin="0,0,0,15">
                                <ui:TextBox Text="{Binding NewVoucherCode}" PlaceholderText="e.g. SUMMER2026" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextOnCardBrush}" CaretBrush="{DynamicResource TextOnCardBrush}"/>
                            </Border>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="Discount (%)" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SlateLightBrush}" Margin="0,0,0,5"/>
                                    <Border Height="40" Background="White" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="6">
                                        <ui:TextBox Text="{Binding NewVoucherDiscountPercent}" PlaceholderText="0" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextOnCardBrush}" CaretBrush="{DynamicResource TextOnCardBrush}"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="Min Spend (đ)" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SlateLightBrush}" Margin="0,0,0,5"/>
                                    <Border Height="40" Background="White" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="6">
                                        <ui:TextBox Text="{Binding NewVoucherDiscountAmount}" PlaceholderText="0" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextOnCardBrush}" CaretBrush="{DynamicResource TextOnCardBrush}"/>
                                    </Border>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Expiry Date" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SlateLightBrush}" Margin="0,0,0,5"/>
                            <Border Height="40" Background="White" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="6" Margin="0,0,0,20">
                                <ui:TextBox Language="vi-VN" Text="{Binding NewVoucherExpiryDate, StringFormat='dd/MM/yyyy', ConverterCulture='vi-VN'}" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextOnCardBrush}" CaretBrush="{DynamicResource TextOnCardBrush}"/>
                            </Border>

                            <TextBlock Text="Usage Limit" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SlateLightBrush}" Margin="0,0,0,5"/>
                            <Border Height="40" Background="White" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="6" Margin="0,0,0,20">
                                <ui:TextBox Text="{Binding NewVoucherUsageLimit}" PlaceholderText="e.g. 100" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextOnCardBrush}" CaretBrush="{DynamicResource TextOnCardBrush}"/>
                            </Border>
                             <ui:Button Content="CREATE VOUCHER" Command="{Binding CreateVoucherCommand}" Style="{StaticResource PremiumSolidButtonStyle}" HorizontalAlignment="Stretch" Height="40"/>
                        </StackPanel>
                    </ui:Card>

                    <StackPanel Grid.Column="1">
                        <!-- Active Vouchers Card -->
                        <ui:Card Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Active Vouchers" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource TextOnCardBrush}"/>
                                <ScrollViewer MaxHeight="450" VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
                                    <ItemsControl ItemsSource="{Binding ActiveVouchers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,12,8" Padding="12" CornerRadius="8" BorderBrush="#A7F3D0" BorderThickness="1.5" Background="#0A10B981">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding Code}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextOnCardBrush}" Margin="0,0,0,4"/>
                                                            <TextBlock Text="{Binding ExpiryDate, StringFormat='Expires: {0:dd/MM/yyyy}'}" FontSize="11" Foreground="{DynamicResource TextBodyBrush}"/>
                                                        </StackPanel>
                                                        <Button Grid.Column="1" 
                                                                Command="{Binding DataContext.DeleteVoucherCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                                                CommandParameter="{Binding}" 
                                                                Width="36" Height="36"
                                                                Cursor="Hand">
                                                            <Button.Template>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border x:Name="Border" 
                                                                            Background="Transparent" 
                                                                            BorderBrush="Transparent" 
                                                                            BorderThickness="1.5" 
                                                                            CornerRadius="8" 
                                                                            SnapsToDevicePixels="True">
                                                                        <ui:SymbolIcon Symbol="Delete24" Foreground="#EF4444" FontSize="18"/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter TargetName="Border" Property="Background" Value="#FFF5F5"/>
                                                                            <Setter TargetName="Border" Property="BorderBrush" Value="#FEE2E2"/>
                                                                        </Trigger>
                                                                        <Trigger Property="IsPressed" Value="True">
                                                                            <Setter TargetName="Border" Property="Background" Value="#FEE2E2"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Button.Template>
                                                        </Button>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                        </ui:Card>

                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
